function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

import React, { Component } from 'react';
import { requireNativeComponent } from 'react-native';
const KakaoMapManager = requireNativeComponent('KakaoMapView');

class KakaoMapView extends Component {
  constructor(...args) {
    super(...args);

    _defineProperty(this, "_onCenterPointMovedTo", event => {
      if (!this.props.onCenterPointMovedTo) {
        return;
      }

      this.props.onCenterPointMovedTo(event.nativeEvent);
    });
  }

  render() {
    const {
      _onCenterPointMovedTo
    } = this;
    const {
      style
    } = this.props;
    return /*#__PURE__*/React.createElement(KakaoMapManager, {
      style: style,
      onCenterPointMovedTo: _onCenterPointMovedTo
    });
  }

}

export default {
  KakaoMapView
};
//# sourceMappingURL=index.js.map